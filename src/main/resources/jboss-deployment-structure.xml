<?xml version="1.0" encoding="UTF-8"?>
<jboss-deployment-structure xmlns="urn:jboss:deployment-structure:1.3">
    <deployment>
        <exclusions>
            <!-- Исключаем старые javax модули (если есть) -->
            <module name="javax.faces.api" slot="main"/>
            <module name="com.sun.jsf-impl" slot="main"/>
        </exclusions>
        <dependencies>
            <!-- Подключаем модуль Oracle JDBC -->
            <module name="com.oracle" services="import"/>

            <!-- Hibernate и JPA модули -->
            <module name="org.hibernate" export="true"/>
            <module name="org.hibernate.orm" services="import"/>

            <!-- CDI -->
            <module name="jakarta.enterprise.api" export="true"/>

            <!-- Servlet API (уже есть в WildFly) -->
            <module name="jakarta.servlet.api" export="true"/>

            <!-- JSF API (PrimeFaces требует это) -->
            <module name="jakarta.faces.api" export="true"/>
        </dependencies>
    </deployment>
</jboss-deployment-structure>